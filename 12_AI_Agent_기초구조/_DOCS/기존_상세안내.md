# AI Agent 기초 구조

Agent의 입력, 도구 호출, 결과 반환 흐름을 한 파일로 살펴보는 최소 예제입니다.

```powershell
pip install -r requirements.txt
Copy-Item .env.example .env
python src/agentic_agent.py
```

이 단계의 완료 범위와 다음 실습 연결은 `STATUS.md`에 정리되어 있습니다.
