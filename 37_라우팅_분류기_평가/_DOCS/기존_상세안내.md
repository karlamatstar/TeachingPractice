# 라우팅 분류기 평가

라우팅 데이터 생성, 분류기 학습, 일반·엣지 케이스 평가를 단계별로 수행합니다.

```powershell
pip install -r requirements.txt
python src/create_dataset.py
python src/train_classifier.py
python src/evaluate_classifier.py
python src/edge_case_test.py
```

학습·평가 데이터는 `data`, 모델과 보고서는 `_OUTPUT`에 저장됩니다.
