# RAG 챗봇 자동 품질평가

문서 수집, 벡터 검색, RAG 응답과 자동 품질평가를 연결하는 실습입니다.

```powershell
pip install -r requirements.txt
streamlit run src/app.py
```

평가 실행은 `python run_evaluation.py`를 사용합니다. 입력 테스트는 `data`, 보고서는 `_OUTPUT`, 기존 상세 설명은 `_DOCS/기존_README.md`에 있습니다.
