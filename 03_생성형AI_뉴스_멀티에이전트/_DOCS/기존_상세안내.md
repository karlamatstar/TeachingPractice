# 생성형 AI 뉴스 멀티에이전트

뉴스 수집, 요약, 분류, 보고서 생성을 여러 Agent 역할로 나눈 실습입니다.

## 실행

```powershell
pip install -r requirements.txt
Copy-Item .env.example .env
python src/main.py
```

생성된 보고서는 `_OUTPUT`에 저장되며 환경설정 참고 문서는 `_DOCS`에 있습니다.
