# LLM as a Judge 최소 예제

LLM 평가자의 입력과 출력 구조를 단일 파일로 확인하는 기초 실습입니다.

```powershell
pip install -r requirements.txt
Copy-Item .env.example .env
python src/evaluators_demo.py
```

후속 품질 파이프라인과의 연결은 `STATUS.md`를 확인합니다.
