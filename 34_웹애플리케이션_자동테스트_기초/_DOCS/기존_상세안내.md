# 웹 애플리케이션 QA 자동화

React/Cypress 프론트엔드와 Node/Jest 백엔드의 기능·통합 테스트 실습입니다.

## 실행

```powershell
Copy-Item .env.example .env
cd backend
npm install
npm test
```

프론트엔드는 `frontend`에서 별도로 설치·실행합니다. API 설명과 모듈 재설치 기록은 `_DOCS`에 있습니다.
