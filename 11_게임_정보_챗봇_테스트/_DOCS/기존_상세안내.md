# 블랙박스 게임 가챠 테스트

가챠 관련 챗봇 응답을 8개 품질 지표로 평가하고 대화·판정 결과를 저장하는 실습입니다.

## 실행

```powershell
pip install -r requirements.txt
Copy-Item .env.example .env
python src/main.py
```

`.env`는 프로젝트 최상위에 두며 결과와 로그는 `_OUTPUT`에 생성됩니다.
